[GlobalPermissions]
# Specify the group or user details
# Use "group" for groups and "user" for users
holder_type = group  # or "user"
holder_parameter = GitProtectGroup  # Replace with the group name or user key

# List the global permissions to grant (comma-separated)
permissions = ADMINISTER, USER_PICKER